# =============================================================================
# DevHub Frontend Environment Variables
# =============================================================================
# Copy this file to .env in the frontend directory and fill in the values.
# Never commit .env to version control!
#
# Note: Vite requires the VITE_ prefix for environment variables to be exposed
# to the client-side code.

# =============================================================================
# API Configuration
# =============================================================================

# GraphQL API endpoint
# Default: http://localhost:3000/graphql
# Adjust if the backend runs elsewhere (e.g., inside Docker or on a remote host)
VITE_GRAPHQL_ENDPOINT=http://localhost:3000/graphql

# REST API base URL
# Default: http://localhost:3000
# Used for authentication endpoints (/session, /users, /profile)
# Adjust if the backend runs elsewhere
VITE_API_BASE_URL=http://localhost:3000
